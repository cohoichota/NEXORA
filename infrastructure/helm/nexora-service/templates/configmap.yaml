apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nexora-service.fullname" . }}-config
  labels:
    {{- include "nexora-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
