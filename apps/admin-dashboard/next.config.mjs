/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@nexora/ui', '@nexora/shared-utils', '@nexora/shared-types'],
};

export default nextConfig;
