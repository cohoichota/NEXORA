{
  "name": "@nexora/kafka-events",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@nexora/tsconfig": "workspace:*",
    "typescript": "^5.4.0"
  }
}
